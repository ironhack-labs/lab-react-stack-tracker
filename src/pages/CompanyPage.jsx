import { useParams } from "react-router-dom";

function CompanyPage() {

  const { companyName } = useParams()

  const companyToDisplay = all

  return (
    <div>
      <h1>Company Profile</h1>
    </div>
  );
}

export default CompanyPage;
