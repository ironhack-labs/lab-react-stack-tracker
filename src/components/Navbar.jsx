import { Link } from "react-router-dom";

function Navbar() {
  return (
    
      <Link to="/">
        <nav className="nav-bar">StackTracker</nav>
      </Link>
    
    );
}

export default Navbar;
